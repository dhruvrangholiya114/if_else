#include<stdio.h>

main()

{
	int a=7;
	if (a==0)
	{
		printf("Given number %d is Neutral",a);
	}
	
	else if (a<0)
	{
		printf("Given number %d is Negative",a);
	}
	else if (a>0)
	{
		printf("Given number %d is Positive",a);
	}
}
